import { ScrollView } from 'react-native'

const ForgotPassword = () => {
  return (
    <ScrollView contentContainerClassName="flex align-center justify-center min-h-screen bg-red-500">
    </ScrollView>
  )
}

export default ForgotPassword
